# 🪝 Custom React Hook — `useFetch`

A lightweight, reusable **custom React hook** for fetching data from APIs with built-in loading and error management.  
Designed for cleaner and more readable data-fetching logic in functional components.

---

## 🚀 Features

- 🧠 Simplifies `fetch()` calls in React  
- ⚡ Automatically handles loading and error states  
- 🔁 Provides a `refetch` method to reload data on demand  
- 🧩 Fully reusable across multiple components  
- ✨ Lightweight — no external dependencies except React

---
